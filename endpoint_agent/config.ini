# EDTMRS Endpoint Agent Configuration
# Edit SERVER_HOST to your Admin PC's IP address
# For same PC testing: SERVER_HOST=127.0.0.1
# For two PC setup: SERVER_HOST=192.168.1.10 (use ipconfig to find Admin PC IP)

SERVER_HOST=127.0.0.1
SERVER_PORT=8000
HEARTBEAT_INTERVAL=30
